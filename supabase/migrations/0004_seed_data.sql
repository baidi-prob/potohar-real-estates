-- =====================================================================
-- 0004_seed_data.sql
--
-- No seed / sample listings are inserted. The marketplace starts empty:
-- every listing is created by a signed-in user through the app.
-- (Kept as an intentional no-op so the migration set stays complete.)
-- =====================================================================
select 1;
